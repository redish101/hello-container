apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-container.fullname" . }}
  labels:
    {{- include "hello-container.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-container.selectorLabels" . | nindent 4 }}
